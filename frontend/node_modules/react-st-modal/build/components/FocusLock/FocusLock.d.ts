import React from 'react';
declare type FocusLockProps = {
    isLocked?: boolean;
    isOpen: boolean;
    children?: React.ReactNode;
};
export default function FocusLock(props: FocusLockProps): JSX.Element;
export {};
